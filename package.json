{
  "name": "omp-jev-router",
  "version": "0.4.0",
  "description": "Jev for omp: the official typesafe/jev-router model does the routing; this plugin adds the host-side tool gate and post-run verification",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "check:release": "./scripts/check-release.sh"
  },
  "license": "MIT",
  "author": {
    "name": "diego-ruas"
  },
  "repository": "github:diego-ruas/omp-jev-router",
  "keywords": [
    "omp-plugin",
    "pi-extension",
    "jev",
    "model-router"
  ],
  "omp": {
    "extensions": [
      "./extensions/jev-router.ts"
    ]
  },
  "peerDependencies": {
    "@oh-my-pi/pi-coding-agent": "^18.0.0"
  }
}
